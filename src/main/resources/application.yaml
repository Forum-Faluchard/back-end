spring:
  application:
    name: backend-forum-faluchard
  datasource:
    url: jdbc:h2:mem:mydb
    username: user
    password: user
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

server:
  port: 38141
